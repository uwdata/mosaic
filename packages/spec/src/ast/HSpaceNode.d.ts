export interface SpecHSpace {
  /**
   * Horizontal space to place between components.
   * Number values indicate screen pixels.
   * String values may use CSS units (em, pt, px, etc).
   */
  hspace: number | string;
}
