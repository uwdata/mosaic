{
  "name": "duckdb-server",
  "version": "0.8.0",
  "private": true,
  "scripts": {
    "prepublishOnly": "hatch run test:cov && hatch fmt --check && hatch build",
    "publish": "hatch publish --user __token__"
  }
}
