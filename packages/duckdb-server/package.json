{
  "name": "duckdb-server",
  "version": "0.7.0",
  "private": true,
  "scripts": {
    "prepublishOnly": "hatch run test:cov && hatch fmt --check && hatch build",
    "publish": "hatch publish"
  }
}
