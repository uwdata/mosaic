{
  "name": "vgplot-python",
  "version": "0.28.1",
  "private": true,
  "scripts": {
    "prerelease": "uv run ruff check && uv run ruff format --check",
    "release": "uv build && uvx twine upload --skip-existing ../../../dist/vgplot*"
  }
}
