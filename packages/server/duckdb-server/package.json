{
  "name": "duckdb-server",
  "version": "0.31.0",
  "private": true,
  "scripts": {
    "dev": "uv run watchmedo auto-restart --pattern '*.py' --recursive --signal SIGTERM uv run duckdb-server"
  }
}
