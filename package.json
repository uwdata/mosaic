{
  "private": true,
  "name": "mosaic",
  "description": "Scalable and extensible linked data views.",
  "license": "BSD-3-Clause",
  "author": "Jeffrey Heer (http://idl.cs.washington.edu)",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/uwdata/mosaic.git"
  },
  "scripts": {
    "build": "lerna run build",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "server": "node bin/run-server.js",
    "vgplot": "cd packages/vgplot && npm run build",
    "dev": "vite",
    "release": "npm run test && npm run lint && lerna publish"
  },
  "devDependencies": {
    "esbuild": "^0.17.6",
    "eslint": "^8.33.0",
    "lerna": "^6.4.1",
    "mocha": "^10.2.0",
    "rimraf": "^4.1.2",
    "timezone-mock": "^1.3.6",
    "vite": "^4.1.1",
    "yaml": "^2.2.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
